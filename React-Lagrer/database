import mysql from 'mysql';

// Create a cache of connections to the mysql server.
// Read more about connection pools here: https://en.wikipedia.org/wiki/Connection_pool
export let pool = mysql.createPool({
  host: 'mysql-ait.stud.idi.ntnu.no',
  connectionLimit: 1, // Limit the number of simultaneous connections to avoid overloading the mysql server
  user: 'omerj', // Replace "username" with your mysql-ait.stud.idi.ntnu.no username
  password: 'tzoJlBGq', // Replae "password" with your mysql-ait.stud.idi.ntnu.no password
  database: 'omerj' // Replace "username" with your mysql-ait.stud.idi.ntnu.no username
});









<?php
function OpenCon()
 {
 $dbhost = 'mysql-ait.stud.idi.ntnu.no';
 $dbuser = "omerj";
 $dbpass = "tzoJlBGq";
 $db = "Scoreboard";
 $conn = new mysqli($dbhost, $dbuser, $dbpass,$db) or die("Connect failed: %s\n". $conn -> error);

 return $conn;
 }

function CloseCon($conn)
 {
 $conn -> close();
 }

?>
